# navinvent.github.io

Articles hub built with MkDocs Material. Add an article by dropping a Markdown
file in `docs/articles/posts/` and pushing to `main` — the Action rebuilds and
publishes automatically.
